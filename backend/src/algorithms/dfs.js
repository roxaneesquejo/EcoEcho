/**
 * Depth-first search placeholder.
 * @param {Record<string, string[]>} graph - adjacency list
 * @param {string} start
 * @returns {string[]}
 */
function dfs(graph, start) {
  // TODO: implement DFS
  return [start];
}

module.exports = { dfs };
